name: Publish Server to Clojars

on: workflow_dispatch

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven
          server-id: clojars
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD
      - name: Store gpg key
        run: echo $GPG_SIGNING_KEY_BASE64 | base64 -d > /tmp/gpg.key
        env:
          GPG_SIGNING_KEY_BASE64: ${{ secrets.GPG_SIGNING_KEY }}
      - name: Build and deploy modules to Clojars
        run: ./mvnw -B -pl '!simple-app' clean package gpg:sign install deploy -Dgpg.signer=bc -Dgpg.keyFilePath=/tmp/gpg.key
        env:
          MAVEN_GPG_PASSPHRASE: ${{ secrets.GPG_SIGNING_PASSPHRASE }}
          MAVEN_USERNAME: grayskull-dev
          MAVEN_PASSWORD: ${{ secrets.DEPLOY_TOKEN }}
